#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

if [ -z "$WAYLAND_DISPLAY" ] && [ -n "$XDG_VTNR" ] && [ "$XDG_VTNR" -eq 1 ] ; then
    exec sway
fi

# Source profile
#[[ -f ~/.profile ]] && . ~/.profile


source /home/b0nz1cu5/.config/broot/launcher/bash/br
